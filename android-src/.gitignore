.gradle/
build/
*/build/
local.properties
.idea/
*.iml
# Gradle wrapper jar is generated by `gradle wrapper` on first build.
# Ship the wrapper scripts (gradlew / gradlew.bat) and gradle-wrapper.properties
# — skip the jar since it's recreated and bloats the repo.
gradle/wrapper/gradle-wrapper.jar
